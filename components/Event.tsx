// eslint-disable-next-line
import classes from "../styles/Event.module.scss";

const Event: React.FC<{ imgSrc: string, title: string, description: string, time: string }> = (props) => {

    return (
        <>

        </>
    )
}

export default Event;