// eslint-disable-next-line
import classes from "../styles/EventCard.module.scss";

const Event: React.FC<{ imgSrc: string, title: string, description: string, time: string }> = ({imgSrc, title, description, time}) => {

    return (
        <>

        </>
    )
}

export default Event;