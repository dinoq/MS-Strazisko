
export enum ComponentType {
    TextField,
    SelectBox,
    UNKNOWN,
    DateField,
    NumberField,
    ImagePreview,
    FileChooser,
    RichTextField
}

export enum DetailFrameMode {
    NEW_ENTRY,
    EDITING_ENTRY
}