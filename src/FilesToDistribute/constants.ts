
export enum DetailFrameComponentType {
    TextField,
    SelectBox,
    DateField,
    FileChooser,
    RichTextField,
    UNKNOWN,
}

export enum ListFrameComponentType {
    Text,
    Link,
    Date,
    ImagePreview,
    RichTextField,
    UNKNOWN,
}

export enum DetailFrameMode {
    NEW_ENTRY,
    EDITING_ENTRY
}